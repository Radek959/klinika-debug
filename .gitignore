node_modules/
dist/
coverage/
.env
.env.*
!.env.example
*.log
# Syntetyczne fixture'y logów warsztatowych są celowo częścią repozytorium
# (workshop-log-fixtures) — wyjątek od ogólnej reguły *.log powyżej.
!workshop-assets/logs/*.log
*.tsbuildinfo
# Dokumentacja produktowa skopiowana przez build do apps/web/public — jedyne
# źródło prawdy pozostaje docs/dokumentacja-produktowa.md.
apps/web/public/materials/docs/
# Artefakty npm run test:workshop-browser (Playwright): raporty i trace'y,
# nigdy nie commitowane.
test-results/
playwright-report/
blob-report/
